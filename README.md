1) Для сборки проекта сроздать фиал paths в корневом коталоге и указать путь до тулчена. Например TOOLS=/bin/, где /bin/ 
путь до gcc
2) Если не получится заинклюдить #include <pcap.h>. Необходимо установить пакет. Пример для Ubuntu sudo apt-get install libpcap-dev.